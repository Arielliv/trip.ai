// useLocationData.js
import { useState } from 'react';
import { MapMarker } from '@/app/components/Map/Map';

export interface TripContextObject {}

export const useTripData = (): TripContextObject => {
  return {};
};
